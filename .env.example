# Supabase（不可轉移負載預測資料來源）
# 不設定的話，程式會用 src/api/supabase.js 裡的預設值（本專題的專案）。
#
# 這裡放的是 anon / publishable key：資料表已開 RLS 只允許 SELECT，
# 所以放進前端是安全的。可寫的 service_role key 絕對不要放這裡。
VITE_SUPABASE_URL=https://xxxx.supabase.co
VITE_SUPABASE_KEY=sb_publishable_xxxxxxxx
